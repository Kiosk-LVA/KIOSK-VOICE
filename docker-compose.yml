version: '3.8'

services:
  valtec-tts:
    build: .
    ports:
      - "7860:7860"
    volumes:
      - ./outputs:/app/outputs
    environment:
      - PYTHONUNBUFFERED=1
    deploy:
      resources:
        limits:
          memory: 4G
